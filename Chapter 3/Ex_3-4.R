#we want to check what happens if we store different type of data is atomic vector
card <- c("ace", "heart", 1)
card
#it converted 1 to character string.
#R will convert the elements to single type of data